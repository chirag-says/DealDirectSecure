import RewardsDashboardContent from './RewardsDashboardContent';

export const metadata = {
    title: 'My Rewards — DealDirect',
    description: 'View your DealDirect Rewards wallet, tier status, referral code, and transaction history.',
};

export default function RewardsDashboardPage() {
    return <RewardsDashboardContent />;
}
