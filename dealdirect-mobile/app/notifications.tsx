import { Placeholder } from '@/dev/Placeholder';

export default function NotificationsScreen() {
  return (
    <Placeholder
      title="Notifications"
      milestone="M5"
      description="GET /notifications, capped at 100 and not paginated."
    />
  );
}
